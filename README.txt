CMDs:

docker build -t hello-project .
docker run hello-project
docker-compose up
docker-compose down --rmi all

section 1
===========

- what is docker ? : it consistently build, run and ship applications
- VMs vs containers
- architecture of docker
- install docker
- developpment workflow